plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace = "com.uragiristereo.mikansei.feature.search.mock"
}

kotlin {
    jvmToolchain(17)
}

dependencies {
    implementation(project(":core:domain"))
    implementation(project(":feature:search"))
}
