package com.uragiristereo.mejiboard.domain.entity.booru.tag

import androidx.compose.runtime.Stable

@Stable
enum class TagType {
    GENERAL,
    CHARACTER,
    COPYRIGHT,
    ARTIST,
    METADATA,
    NONE,
}
