<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Mejiboard.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, and icon. -->
        <item name="windowSplashScreenBackground">@color/black</item>

        <!-- Set the theme of the Activity that directly follows your splash screen. -->
        <!-- Required -->
        <item name="postSplashScreenTheme">@style/Theme.MejiboardComposer</item>
    </style>

    <style name="Theme.MejiboardComposer" parent="Theme.AppCompat.Light.NoActionBar" />
</resources>
